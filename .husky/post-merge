# .husky/post-merge (and/or post-checkout)
#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"
set -e
if git diff --name-only HEAD@{1}..HEAD | grep -qE '(^|/)(bun\.lock|package-lock\.json|pnpm-lock\.yaml|yarn\.lock)$'; then
  bun install
fi
